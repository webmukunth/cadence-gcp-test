rootProject.name = "magneto"
include "commons-api"
include "processfile-api"
include "processfile-wf-client"
include "processfile-web"

rootProject.children.each {project ->
    project.buildFileName = "${project.name}.gradle"
}

